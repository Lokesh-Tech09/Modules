"""
VeriFace — Root requirements.txt
Consolidated dependencies for all three modules + integration layer.

Install with:
    python -m venv .venv
    source .venv/bin/activate          # Windows: .venv\\Scripts\\activate
    pip install -r requirements.txt
"""

# ── Module 1: Face Identification ──────────────────────────────────────────
insightface>=0.7.3
onnxruntime>=1.16.0
opencv-python>=4.8.0
numpy>=1.24.0
pillow>=10.0.0

# ── Module 2: Web/Post Verification ────────────────────────────────────────
pydantic>=2.0.0
httpx>=0.27.0
requests>=2.28.0
beautifulsoup4>=4.12.0

# ── Module 3: Blockchain ────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Shared: Genuine Search / Discovery ────────────────────────────────────
ddgs>=9.0.0                     # Current package name (was: duckduckgo-search)
duckduckgo-search>=4.0.0        # Legacy name — kept for compatibility

# ── Testing ────────────────────────────────────────────────────────────────
pytest>=7.0.0
pytest-asyncio>=0.23.0

# ── Optional: Real EVM Blockchain (Sepolia testnet) ───────────────────────
# Uncomment when BLOCKCHAIN_PROVIDER=sepolia and credentials are configured:
# web3>=6.0.0

# ── Optional: SerpAPI Search (paid, requires SERPAPI_KEY in .env) ─────────
# Uncomment to enable SerpAPI adapter:
# google-search-results>=2.4.2
